# BOXY SKITZY TEE IMAGE DROP

Upload the two product images into this folder using these exact filenames:

- `front.jpg`
- `back.jpg`

The merch page is already wired to those paths. Replace these instructions with additional files later if needed, but keep the two filenames above unchanged.
